# Configuración de optimización para VRTon - SEO Enhanced
# Enable mod_rewrite
RewriteEngine On

# Security Headers for SEO
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options DENY  
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
    
    # Content Security Policy
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; img-src 'self' data: https: https://flagcdn.com; connect-src 'self'; font-src 'self' https://cdnjs.cloudflare.com; media-src 'self'"
    
    # Cache Control Headers
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|svg|woff|woff2|ttf|eot|mp4|webm)$">
        Header set Cache-Control "max-age=2592000, public"
    </FilesMatch>
    
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "max-age=604800, public, must-revalidate"
    </FilesMatch>
    
    <FilesMatch "\.(json)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires 0
    </FilesMatch>
</IfModule>

# WWW to non-WWW redirect
RewriteCond %{HTTP_HOST} ^www\.vrton\.org [NC]
RewriteRule ^(.*)$ https://vrton.org/$1 [L,R=301]

# Custom 404 Error Page
ErrorDocument 404 /404.html

# Compresión GZIP
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE application/ld+json
</IfModule>

# Cache de navegador
<IfModule mod_expires.c>
    ExpiresActive on
    
    # Imágenes
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Videos
    ExpiresByType video/mp4 "access plus 1 month"
    ExpiresByType video/webm "access plus 1 month"
    
    # CSS y JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    
    # HTML
    ExpiresByType text/html "access plus 1 hour"
    
    # Fuentes
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    
    # Manifiest y Service Worker
    ExpiresByType application/manifest+json "access plus 1 week"
    ExpiresByType text/cache-manifest "access plus 0 seconds"
</IfModule>

# Headers de cache
<IfModule mod_headers.c>
    # Cache para recursos estáticos
    <FilesMatch "\.(ico|pdf|flv|jpg|jpeg|png|gif|webp|js|css|swf)$">
        Header set Cache-Control "max-age=2592000, public"
    </FilesMatch>
    
    # No cache para HTML
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "max-age=3600, public"
    </FilesMatch>
    
    # Service Worker no cache
    <FilesMatch "sw\.js$">
        Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "Wed, 11 Jan 1984 05:00:00 GMT"
    </FilesMatch>
    
    # Seguridad
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # CSP básico - removed via.placeholder.com as we now use local placeholders
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' cdnjs.cloudflare.com; img-src 'self' data: flagcdn.com; font-src 'self' cdnjs.cloudflare.com; connect-src 'self'; media-src 'self';"
</IfModule>

# Redirecciones y URLs amigables
RewriteEngine On

# Forzar HTTPS (descomentar si tienes SSL)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirigir index.html a /
RewriteCond %{THE_REQUEST} /11-index\.html[\s?] [NC]
RewriteRule ^11-index\.html$ / [R=301,L]

# Servir 11-index.html para la raíz (temporal hasta renombrar a index.html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^$ 11-index.html [L]

# Remover extensión .html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Language Negotiation for SEO
Options +MultiViews
LanguagePriority es en
ForceLanguagePriority Prefer Fallback

# Redirect .html to clean URLs for better SEO
RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
RewriteRule ^ /%1? [R=301,L]

# Sitemap and robots.txt location
RewriteRule ^sitemap\.xml$ /sitemap.xml [L]
RewriteRule ^robots\.txt$ /robots.txt [L]

# Block access to sensitive files
<FilesMatch "(^#.*#|\.(bak|config|dist|fla|inc|ini|log|psd|sh|sql|sw[op])|~)$">
    Order allow,deny
    Deny from all
    Satisfy All
</FilesMatch>

# Block access to development files for security
RedirectMatch 404 /\.git
RedirectMatch 404 /\.vscode
RedirectMatch 404 /node_modules
RedirectMatch 404 /package\.json
RedirectMatch 404 /package-lock\.json
RedirectMatch 404 /README\.md
RedirectMatch 404 /LICENSE
RedirectMatch 404 /CONTRIBUTING\.md
RedirectMatch 404 /optimize-video\.ps1

# MIME Types for better SEO and performance
AddType application/vnd.ms-fontobject .eot
AddType font/ttf .ttf
AddType font/otf .otf
AddType font/woff .woff
AddType font/woff2 .woff2
AddType image/svg+xml .svg
AddType image/webp .webp
AddType application/manifest+json .webmanifest

# Enable ETags for better caching
FileETag MTime Size

# Remove server signature for security
ServerTokens Prod

# Error 404 personalizado
ErrorDocument 404 /404.html

# Prevenir acceso a archivos sensibles
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|inc|bak)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Bloquear acceso a directorios de sistema
RedirectMatch 404 /\.git
RedirectMatch 404 /\.idea
RedirectMatch 404 /node_modules

# Optimización de rendimiento
<IfModule mod_mime.c>
    # Tipos MIME para fuentes web
    AddType application/font-woff woff
    AddType application/font-woff2 woff2
    
    # Tipo MIME para manifest
    AddType application/manifest+json webmanifest
    
    # Tipos MIME para videos
    AddType video/mp4 mp4
    AddType video/webm webm
</IfModule>

# Preload crítico
<IfModule mod_headers.c>
    <FilesMatch "11-index\.html$">
        Header add Link "</styles.min.css>; rel=preload; as=style"
        Header add Link "</assets/logo.png>; rel=preload; as=image"
    </FilesMatch>
</IfModule>
