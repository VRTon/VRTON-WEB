class CountdownTimer { constructor() { this.targetDate = new Date("2025-11-28T22:00:00-03:00"), this.elements = { days: document.getElementById("days"), hours: document.getElementById("hours"), minutes: document.getElementById("minutes"), seconds: document.getElementById("seconds") }, this.init() } init() { this.elements.days && this.elements.hours && this.elements.minutes && this.elements.seconds ? (this.updateCountdown(), this.interval = setInterval(() => { this.updateCountdown() }, 1e3)) : console.warn("Elementos del contador no encontrados") } updateCountdown() { const e = (new Date).getTime(), t = this.targetDate.getTime() - e; if (t < 0) return void this.showEventCompleted(); const n = Math.floor(t / 864e5), o = Math.floor(t % 864e5 / 36e5), s = Math.floor(t % 36e5 / 6e4), d = Math.floor(t % 6e4 / 1e3); this.animateNumber(this.elements.days, n), this.animateNumber(this.elements.hours, o), this.animateNumber(this.elements.minutes, s), this.animateNumber(this.elements.seconds, d) } animateNumber(e, t) { (parseInt(e.textContent) || 0) !== t && (e.style.transform = "scale(1.1)", e.style.color = "var(--accent-color)", setTimeout(() => { e.textContent = t.toString().padStart(2, "0"), e.style.transform = "scale(1)", e.style.color = "" }, 150)) } showEventCompleted() { clearInterval(this.interval); const e = document.getElementById("countdown-timer"), t = document.querySelector(".countdown-title"), n = document.getElementById("event-info-container"); e && (e.style.display = "none"), t && (t.style.display = "none"), n && (n.style.display = "block") } destroy() { this.interval && clearInterval(this.interval) } } document.addEventListener("DOMContentLoaded", () => { setTimeout(() => { document.getElementById("countdown-timer") ? (window.countdownTimer = new CountdownTimer, window.onCountdownReady && window.onCountdownReady()) : window.onCountdownReady && window.onCountdownReady() }, 100) }), window.addEventListener("beforeunload", () => { window.countdownTimer && window.countdownTimer.destroy() });